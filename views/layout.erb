<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Twitter Two</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
  </head>
  <body>
    <nav>
      <!-- <a href="/login">login</a> -->
      <% if !session[:login] %>
      <a href="<%= current_user == nil ? '/login' : '/logout' %>">
        <%= current_user == nil ? 'Login' : 'Logout' %>
      </a>
      <p style="color: grey"> Current user: <%= current_user != nil ? @user.username : "" %></p>
      <% end %>
    </nav>

    <%= yield %>
    <script src="/js/index.js"></script>
  </body>
</html>
